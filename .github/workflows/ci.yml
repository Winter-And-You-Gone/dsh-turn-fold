name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: node --check index.js
      - run: node --check client.js
      - run: node --check verify-fix.mjs
      - run: npm run check
      - run: npm pack --dry-run
